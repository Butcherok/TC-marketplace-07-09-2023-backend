const {
    errorMessage,
    serverMessage,
    file,
    fileFormats,
    emailRegex,
    phoneRegex,
    passwordRegex,
    schemaMessage,
    cityRegex,
    defaultAvatarUrl,
    userFieldType,
    emailFrom
  } = require("./constants");
  
  module.exports = {
    errorMessage,
    serverMessage,
    file,
    fileFormats,
    emailRegex,
    phoneRegex,
    passwordRegex,
    schemaMessage,
    cityRegex,
    defaultAvatarUrl,
    userFieldType,
    emailFrom
  };
  